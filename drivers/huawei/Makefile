#=============================================================================
#=============================================================================

obj-y += hsad/

obj-y += device/

obj-$(CONFIG_HUAWEI_GPIO) += gpio/

obj-$(CONFIG_HW_POWER_TREE) += power_config/
